export type Classroom = {
    id?: string,
    active?: boolean,
    description?: string,
    gradeId?: string,
    name?: string,
    createdBy?: string,
    created?: number,
    updated?: number
}